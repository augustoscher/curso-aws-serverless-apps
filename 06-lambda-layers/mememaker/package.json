{
  "name": "mememaker",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "invoke-local": "sls invoke local -f mememaker --docker --logger",
    "invoke": "sls invoke -f mememaker --logger --path mocks/request.json"
  },
  "license": "MIT",
  "dependencies": {
    
  }
}
