{
  "name": "lambda-environment",
  "version": "1.0.0",
  "main": "handler.js",
  "license": "MIT",
  "scripts": {
    "invoke-local": "sls invoke local -f commit-message-scheduler",
    "invoke-local-log": "sls invoke local -f commit-message-scheduler --logger",
    "invoke": "sls invoke  -f commit-message-scheduler",
    "deploy": "sls deploy",
    "deploy:qa": "sls deploy --stage qa",
    "remove": "sls remove"
  },
  "dependencies": {
    "aws-sdk": "^2.814.0",
    "axios": "^0.19.2",
    "cheerio": "^1.0.0-rc.3",
    "env-var": "^6.1.0"
  }
}
